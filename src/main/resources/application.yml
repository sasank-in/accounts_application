spring:
  application:
    name: accounts
  datasource:
    url: jdbc:postgresql://localhost:5432/eazybytes
    username: sasank
    password: 23eOfdS%~P,wo[GG
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: app_schema
        format_sql: true

server:
  port: 8000
